export default class SDDN {
    /** @type {number} */
    SDDSID
    /** @type {string} */
    SDDPN
    /** @type {string} */
    SDDPA

    constructor(SDDSID, SDDPN, SDDPA) {
        this.SDDSID = SDDSID
        this.SDDPN = SDDPN
        this.SDDPA = SDDPA
    }
}